services:
  - type: web
    name: radconsent
    runtime: docker
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        generateValue: true
    autoDeploy: true
